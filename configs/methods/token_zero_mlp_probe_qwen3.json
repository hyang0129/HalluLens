{
  "name": "token_zero_mlp_probe_qwen3",
  "extends": "token_zero_mlp_probe",
  "_comment": "Qwen3-8B counterpart of token_zero_mlp_probe. The training/scoring recipe is unchanged; the pooled depth trajectory spans all 36 captured Qwen post-block layers instead of Llama's 32 (relevant_layers='1-32' would silently drop the top 4 layers for Qwen).",
  "data": {
    "relevant_layers": "1-36"
  }
}
