development
.env
.venv/

# Shared data directory (large files, not synced) — symlink to ../shared
shared
shared/

# Per-sample prediction cache (large files; shipped as a GitHub release asset
# rather than tracked in git). Re-fill via scripts/assemble_predictions_tarball.py
# on the cluster + scp + extract.
results/preds/

# Root-level notebooks (all notebooks should live in notebooks/)
/*.ipynb

# Jupyter checkpoints
.ipynb_checkpoints/

# Debug/model artifacts
debug_dumps/
models/

# Misc untracked
requirements2.txt
requirements3.txt

# Ad-hoc agent debugging scripts — never commit
/.agent-work/

# Log files
logfile.log
logfiledebug*.log
*.log

data/wikirank/*.txt
data/wiki_data/*.jsonl
# data/wiki_data/*.txt
data/wiki_data/.cache/*
data/nonexistent_refusal
scripts/data_download-full.sh

__pycache__/
data/longwiki
data/nonsense_all
data/precise_qa

# Large datasets and generated activation stores
data/**
custom_output/
checkpoints/
json_dummy_data/
zarr_dummy_data/
goodwiki_json_2/
test_precisewiki_logging/
test_triviaqa_format/

debug.ipynb

output/*
venv/
generation.jsonl
test_llama.json

# Testing and debugging files
goodwiki_json/
test_output/
**/test_*.json
**/test_*.jsonl
**/failed_request_*.json
**/skipped_samples.json
client.log
server.log
*_client.log
*_server.log
metadata.json
generation(1).jsonl
nq_logprob_recon_contrastive_training.ipynb

# Memmap preload cache (generated alongside zarr stores)
_memmap_cache/

# Experiment run outputs
runs/
runs_archive/

# Exploratory work (scratch notebooks, experiments)
exploratory_work/

# Claude Code agent worktrees (temporary git worktrees created by sub-agents)
.claude/worktrees/
.claude/scheduled_tasks.lock

# Ephemeral smoketest outputs (logs, GPU samples, time -v dumps)
reports/smoketest_*/

# Ephemeral sampling-baseline run outputs (cell/shard logs, fanout TSVs)
reports/sampling_baselines_runs/

# Ephemeral llmsknow_probe re-run outputs (shard logs, fanout TSVs)
reports/llmsknow_probe_runs/

# Generated experiment-coverage markdown (from scripts/audit_coverage.py)
reports/experiment_coverage_*.md

# SLURM time-left in nodes.json is auto-refreshed by gpu_dispatch.py
# sync-jupyter and constantly dirties the working tree.
configs/nodes.json
